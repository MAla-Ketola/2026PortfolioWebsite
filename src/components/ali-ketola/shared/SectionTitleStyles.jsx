import React from "react";

// Previously contained HUD/terminal styles (scramble text, typed labels, scanlines).
// Now simplified — all styling is handled via Tailwind in ui.jsx components.
export default function SectionTitleStyles() {
  return null;
}
